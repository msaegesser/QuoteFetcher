# QuoteFetcher
Python script to fetch quotes from https://zenquotes.io/

## installation
- clone repository
- create virtual enviroment
- install packages from requirements.txt => `py -m pip install -r requirements.txt`

## usage
- run script with => `py fetch_quote.py`